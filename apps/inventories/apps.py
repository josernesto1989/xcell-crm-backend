from django.apps import AppConfig

# apps/inventories/apps.py

from django.apps import AppConfig

class InventoriesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.inventories'
    
